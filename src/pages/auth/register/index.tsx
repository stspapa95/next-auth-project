import React from "react";
import DefaultLayout from "@/components/DefaultLayout";

const RegisterPage = () => {
  return <DefaultLayout>This is a register page!</DefaultLayout>;
};

export default RegisterPage;
